TEWC : The Easiest Web Chat
A simply web chat made with ExtJS 4 and websocket of HTML5

Requirements
- Apache2 with mod_rewrite
- PHP5
- A browser which supports websockets

Install
- Unpack both client and server folders in a single one
- Change the global 'host' variable in the 'loader.js' file (TEWC/client/loader.js). It must point to the 'server.php' file (TEWC/server/server.php)
- Start the websocket (e.g. php -q /var/www/TEWC-test/server.php)
